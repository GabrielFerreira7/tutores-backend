fastapi==0.141.1
uvicorn[standard]==0.52.3
sqlmodel==0.0.39
pydantic-ai==2.31.1
pydantic-settings==2.15.0
httpx==0.28.1
python-dotenv==1.2.3
slowapi==0.1.10

# Driver do PostgreSQL — descomente se trocar DATABASE_URL de sqlite:// para
# postgresql+psycopg://... (ver README.md, seção "Banco de dados"). Não é necessário
# para o default (SQLite), por isso fica fora da instalação padrão.
# psycopg[binary]==3.2.3

# Dependências de dev/test ficam em requirements-dev.txt (não vão para a imagem Docker
# de produção — ver Dockerfile).
